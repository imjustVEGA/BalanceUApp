<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme - Moderno y Calmante -->
    <style name="Theme.BalanceUApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color - Verde Salvia (Salud) -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryVariant">@color/primary_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand color - Azul Calmante (Calma) -->
        <item name="colorSecondary">@color/secondary_teal</item>
        <item name="colorSecondaryVariant">@color/secondary_teal_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Background colors -->
        <item name="android:colorBackground">@color/background_soft</item>
        <item name="colorOnBackground">@color/text_primary</item>
        <item name="colorSurface">@color/background_white</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorSurfaceVariant">@color/background_dark</item>
        
        <!-- Error color -->
        <item name="colorError">@color/error_soft</item>
        <item name="colorOnError">@color/white</item>
        
        <!-- Material Design 3 enhancements -->
        <item name="android:statusBarColor">@color/primary_green</item>
        <item name="android:navigationBarColor">@color/background_white</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">true</item>
        
        <!-- Text input styling -->
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
        
        <!-- Button styling -->
        <item name="materialButtonStyle">@style/Widget.MaterialComponents.Button</item>
        
        <!-- Card styling -->
        <item name="cardViewStyle">@style/Widget.MaterialComponents.CardView</item>
        
        <!-- Remove ActionBar -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Estilo para botones con esquinas redondeadas -->
    <style name="Widget.MaterialComponents.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">12dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
    
    <!-- Estilo para cards modernos -->
    <style name="Widget.MaterialComponents.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardBackgroundColor">@color/background_white</item>
    </style>

    </resources>